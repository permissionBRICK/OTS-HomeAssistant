Fixes the deprecated `via_device` warning reported in [issue #19](https://github.com/permissionBRICK/OTS-HomeAssistant/issues/19), ahead of its removal in Home Assistant 2027.8.

Heating-circuit devices now use the controller's registered device ID through `via_device_id` across all six entity platforms. Existing device identifiers, entity identifiers and parent relationships are preserved.

Update **Ochsner Local OTS** to **v2.1.1** in HACS, then fully restart Home Assistant. Keep the existing integration entry; no rescan or reconfiguration is required.

Validated on Home Assistant 2026.9.1: **112 tests passed**, with **21 skipped** because historical APK/bundle research files were unavailable. Regression tests cover all six platforms, multiple controllers, existing circuit device reuse, and parent device IDs across simulated address recovery and reload. No live heat-pump test was performed for this patch.
